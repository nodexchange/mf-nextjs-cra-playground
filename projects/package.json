{
  "scripts": {
    "dev": "lerna run --scope @playground/* --parallel dev",
    "build": "lerna run --scope @playground/* --parallel build",
    "start": "lerna run --scope @playground/* --parallel start"
  },
  "dependencies": {
    "lerna": "^3.22.1"
  }
}
